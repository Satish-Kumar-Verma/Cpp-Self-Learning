// Arithmetic operators [+, -, *, /, %] , ++, -- (increment/decrement)

// Assignment operators [=, +=, -=, *=, /=, %=]

// Relational operators [==, !=, >, <, >=, <=]

// Logical operators [&&, ||, !]

// Bitwise operators [&, |, ^, ~, <<, >>]

// Other C++ operators [sizeof(), ?:, &(address of), . (p.name),
// -> (p->name), <<, >>]